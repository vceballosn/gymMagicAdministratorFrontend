export interface Partner {
  id: string; // O number, si tu ID es un número
  // Añade aquí el resto de las propiedades de un socio
  name: string;
  email: string;
  phone: string;
  dateRecord: string;
}